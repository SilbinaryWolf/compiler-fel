package parser

import (
//"fmt"

//"github.com/silbinarywolf/compiler-fel/ast"
//"github.com/silbinarywolf/compiler-fel/token"
)

/*
func (p *Parser) parseComponent() ast.Node {
Loop:
	for {
		t := p.GetNextToken()
		switch t.Kind {
		case token.Identifier:
			name := t.String()
			switch name {
			case "properties":
				panic("parseComponent(): todo properties")
			case "css":
				panic("parseComponent(): todo css")
			case "html":
				panic("parseComponent(): todo html")
			default:
				p.addError(p.expect(t, "properties", "css", "html"))
				break Loop
			}
		case token.Newline:
			// no-op
		default:
			panic(fmt.Sprintf("parseComponent(): Unhandled token: %s on Line %d", t.Kind.String(), t.Line))
		}
	}
	panic(fmt.Sprintf("todo(Jake): Finish parseComponent"))
	return nil
}
*/
